model FeatureFlag {
    id          String   @id @default(uuid())
    name        String
    slug        String
    isEnabled   Boolean
    rollout     Int
    description String
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt

    @@map("featureflag")
}
